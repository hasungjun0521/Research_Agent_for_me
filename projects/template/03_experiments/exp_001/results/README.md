# Experiment exp_001 Results

Store raw and processed outputs for `exp_001` here.

Recommended files:

- `metrics.json` or `metrics.csv`
- `predictions.csv`
- `summary.md`
- diagnostic plots
- sampled failure cases

Large generated outputs are ignored by `.gitignore` for non-template projects. Keep small summaries and metadata in tracked markdown or YAML files.


# Notebooks

Use notebooks for exploration, visualization, and quick checks.

Rules:

- Keep final experiment logic in `src/` when possible.
- Name notebooks with date or experiment ID.
- Record conclusions in markdown files under `03_experiments/` or `05_results/`.
- Do not let notebook-only results become paper claims without reproducible scripts or clear documentation.


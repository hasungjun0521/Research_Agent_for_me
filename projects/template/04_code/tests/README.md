# Tests

Use this folder for lightweight tests and smoke checks.

Suggested checks:

- Data loader returns expected schema.
- Metric implementation matches known examples.
- Baseline can run on a tiny fixture.
- Experiment command creates expected output files.
- Random seed behavior is documented.

Tests should support reproducibility, not create a heavy framework.


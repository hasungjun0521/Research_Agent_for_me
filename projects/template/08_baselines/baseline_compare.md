# Baseline Compare

No baseline source comparison has been generated yet.

## Purpose

Use this file after baseline GitHub/source repositories are cloned into
`08_baselines/source_snapshots/<baseline_id>/`. Compare config, data, model,
training, and evaluation structure before shaping `04_code/src/`.
